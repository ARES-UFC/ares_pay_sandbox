services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/ares_pay.db
    volumes:
      - ares-data:/app/data
    restart: unless-stopped

volumes:
  ares-data:
